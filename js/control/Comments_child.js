/**
 * 登陆相关逻辑
 */
define(
    [
        'bedtime',
        'comp/Reply',
        'base64',
        'template'
    ],
    function (bed, Reply, Base64, template) {
        bed.templateConfig(template);
        var c = {

        };

        return c;


    }
);